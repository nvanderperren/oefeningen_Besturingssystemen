pull_request_rules:
  - name: automatic merge when CultURIze import
    conditions:
      - author:nvanderperren
      - base:master
      - title:Culturize Import
    actions:
      merge:
        method: merge
